class Player {
  constructor(playerNum, playerType, playerBoard) {
    this.playerNum = playerNum; // player1 or player2
    this.playerType = playerType; // human or computer
    this.playerBoard = playerBoard;
  }
}

export { Player };
